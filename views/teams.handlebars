<h1>Equipos de la Premier League</h1>
{{!-- {{#each data.teams as |team|}}
  <li>Team: {{team.name}}</li>
{{/each}} --}}
    <h3> Total equipos: {{data.teamsQuantity}}     </h3> 
    <button class="btn btn-success" onclick="window.location.href='http://localhost:8080/teams/addteam'" >Agregar Equipo</button>

<table class="table table-dark">
  <thead>
    <tr>
      <th scope="col">ID</th>
      <th scope="col">Nombre Equipo</th>
      <th scope="col">País</th>
      <th scope="col">Acción</th>
    </tr>
  </thead>
  <tbody>
    {{#each data.teams as |team|}}
        <tr>
      <th scope="row"id={{team.id}}>{{team.id}}</th>
      <td> <img src="{{team.crestUrl}}" alt="{{team.name}} logo">
        ///
         {{team.name}}

      </td>
      <td>{{team.area.name}}</td>
      <td> <button onclick=window.location.href='http://localhost:8080/teams/{{team.id}}' class="btn btn-warning">Ver</button>
     <button  class="btn btn-warning">Editar</button>
      <button  class="btn btn-warning">Eliminar</button> 

      </td>
    </tr>
   
{{/each}}

    {{!-- <tr>
      <th scope="row">2</th>
      <td>Jacob</td>
      <td>Thornton</td>
      <td>@fat</td>
    </tr>
    <tr>
      <th scope="row">3</th>
      <td colspan="2">Larry the Bird</td>
      <td>@twitter</td>
    </tr> --}}
  </tbody>
</table>